# JWT private key — never commit
django/config/keys/jwt_private.pem
*.pyc
__pycache__/
*.egg-info/
.venv/
dist/
.env
.env.local

# Migration run artifacts (keep the dir via .gitkeep, ignore generated files)
ops/migration/reports/*.json
ops/migration/reports/.*.checkpoint
